import { useEffect, useRef } from 'react';
import { Activity, X } from 'lucide-react';
import { JournalRunsPanel } from './JournalRunsPanel';
import './JournalDrawer.css';
export function JournalLogsDrawer({open,onClose}:{open:boolean;onClose:()=>void}){const close=useRef<HTMLButtonElement>(null),prior=useRef<HTMLElement|null>(null);useEffect(()=>{if(!open)return;prior.current=document.activeElement as HTMLElement;setTimeout(()=>close.current?.focus(),0);const key=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose();if(e.key==='Tab'){const root=close.current?.closest('[role=dialog]');const nodes=root?.querySelectorAll<HTMLElement>('button:not(:disabled),a[href],input:not(:disabled),select:not(:disabled),textarea:not(:disabled),[tabindex]:not([tabindex="-1"])');if(nodes?.length){const first=nodes[0],last=nodes[nodes.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}}};document.addEventListener('keydown',key);return()=>{document.removeEventListener('keydown',key);prior.current?.focus()}},[open,onClose]);if(!open)return null;return <div className="journal-overlay logs-overlay" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><section className="journal-logs-drawer" role="dialog" aria-modal="true" aria-labelledby="journal-logs-title"><header className="drawer-heading"><div className="drawer-heading-copy"><span className="drawer-kicker"><Activity size={14}/> Journal operations</span><h2 id="journal-logs-title">Pipeline logs</h2><p>Review generation status, validation history and available actions.</p></div><button ref={close} className="icon-touch" onClick={onClose} aria-label="Close journal pipeline logs"><X/></button></header><JournalRunsPanel/></section></div>}
